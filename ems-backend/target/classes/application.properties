spring.application.name=ems-backend

server.port=${PORT:8080}

# PostgreSQL (Neon DB)
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-orange-resonance-anww84vg-pooler.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_VfaS7pFo6APC}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

logging.level.root=INFO
logging.level.com.example.ems=INFO

# App config
app.cors.origins=${APP_CORS_ORIGINS:http://localhost:5173,http://localhost:5174,https://ems-frontend-lake-two.vercel.app,https://ems-frontend-hf99ucpap-butti-anand-vardhans-projects.vercel.app,https://ems-frontend-l9bg4b30w-butti-anand-vardhans-projects.vercel.app}
app.bootstrap.admin.email=anand@gmail.com
app.bootstrap.admin.password=Anand@2525
app.bootstrap.default-user-password=${APP_DEFAULT_USER_PASSWORD:Welcome@123}

server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true

# JWT Config
app.jwt.secret=${JWT_SECRET:a-very-secure-default-jwt-secret-key-that-must-be-at-least-32-bytes}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

